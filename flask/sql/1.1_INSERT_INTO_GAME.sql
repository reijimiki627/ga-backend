-- game
-- CREATE TABLE IF NOT EXISTS gadb.game
-- (`id` INT NOT NULL AUTO_INCREMENT,
-- `title` VARCHAR(20) NOT NULL,
-- `type` VARCHAR(10) DEFAULT NULL,
-- `create_date` DATE NOT NULL,
-- PRIMARY KEY ( `id` ));

INSERT INTO gadb.game VALUES (null, 'Apex Legends', '', now());
INSERT INTO gadb.game VALUES (null, 'Splatoon3', '', now());
INSERT INTO gadb.game VALUES (null, 'Dead By Daylight', '', now());
